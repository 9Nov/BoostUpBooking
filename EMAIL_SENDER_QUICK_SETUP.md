# 🎯 Quick Email Sender Setup

## ⚡ การตั้งค่าแบบรวดเร็ว (2 นาที)

### 📝 ขั้นตอนที่ 1: เปิดไฟล์ backend_code.gs

ใน Google Apps Script Editor หาบรรทัดนี้:

```javascript
// ========================================
// 📧 EMAIL CONFIGURATION - แก้ไขที่นี่
// ========================================
```

### ⚙️ ขั้นตอนที่ 2: แก้ไขค่าตามต้องการ

```javascript
// Display Name ที่จะแสดงเป็น sender (แทนชื่อ Google Account)
const SENDER_NAME = "Booking System";  // 👈 เปลี่ยนตรงนี้

// Reply-To Email (email ที่จะได้รับเมื่อผู้รับกด Reply)
const REPLY_TO_EMAIL = null;  // 👈 เปลี่ยนตรงนี้

// No-Reply Mode (ถ้าเป็น true จะไม่สามารถ reply ได้)
const NO_REPLY_MODE = false;  // 👈 เปลี่ยนตรงนี้
```

---

## 📋 ตัวอย่างการตั้งค่า

### ตัวอย่างที่ 1: ธุรกิจทั่วไป
```javascript
const SENDER_NAME = "ABC Company Booking";
const REPLY_TO_EMAIL = "support@abc-company.com";
const NO_REPLY_MODE = false;
```
**ผลลัพธ์:**
- ผู้รับเห็น: `ABC Company Booking <yourname@gmail.com>`
- กด Reply ส่งไปที่: `support@abc-company.com`

---

### ตัวอย่างที่ 2: ภาษาไทย
```javascript
const SENDER_NAME = "ระบบจองห้องประชุม - บริษัท ABC";
const REPLY_TO_EMAIL = "booking@abc.co.th";
const NO_REPLY_MODE = false;
```
**ผลลัพธ์:**
- ผู้รับเห็น: `ระบบจองห้องประชุม - บริษัท ABC <yourname@gmail.com>`
- กด Reply ส่งไปที่: `booking@abc.co.th`

---

### ตัวอย่างที่ 3: No-Reply (ไม่แนะนำ)
```javascript
const SENDER_NAME = "Automated Booking System";
const REPLY_TO_EMAIL = null;
const NO_REPLY_MODE = true;
```
**ผลลัพธ์:**
- ผู้รับเห็น: `Automated Booking System <yourname@gmail.com>`
- มีข้อความ "Please do not reply to this message"

---

### ตัวอย่างที่ 4: แยกตาม Location
```javascript
// สำหรับ Bangkok
const SENDER_NAME = "Bangkok Office - Room Booking";
const REPLY_TO_EMAIL = "bangkok@company.com";
const NO_REPLY_MODE = false;

// หรือสำหรับ Rayong
const SENDER_NAME = "Rayong Office - Room Booking";
const REPLY_TO_EMAIL = "rayong@company.com";
const NO_REPLY_MODE = false;
```

---

## ✅ Checklist หลังแก้ไข

- [ ] แก้ไข `SENDER_NAME` เป็นชื่อที่ต้องการ
- [ ] ตั้งค่า `REPLY_TO_EMAIL` (หรือใส่ `null` ถ้าไม่ต้องการ)
- [ ] ตรวจสอบ `NO_REPLY_MODE` (แนะนำให้เป็น `false`)
- [ ] บันทึกไฟล์ (Ctrl+S)
- [ ] Deploy version ใหม่
- [ ] ทดสอบส่ง email

---

## 🎨 ตัวอย่าง Email ที่ผู้รับจะเห็น

### ก่อนแก้ไข (Default):
```
From: Your Name <yourname@gmail.com>
Subject: ✅ Booking Confirmation - Bangkok on 2026-01-26
```

### หลังแก้ไข:
```
From: ABC Company Booking <yourname@gmail.com>
Reply-To: support@abc-company.com
Subject: ✅ Booking Confirmation - Bangkok on 2026-01-26
```

---

## ⚠️ ข้อควรระวัง

### ❌ สิ่งที่ทำไม่ได้:
- เปลี่ยน email address ที่ส่ง (ต้องเป็น account ที่ deploy)
- ส่งจาก email อื่นที่ไม่ใช่ account ที่ deploy (ต้องใช้ Google Workspace)

### ✅ สิ่งที่ทำได้:
- เปลี่ยน Display Name (ชื่อที่แสดง)
- กำหนด Reply-To Email (email ที่จะได้รับเมื่อผู้รับกด Reply)
- เปิด/ปิด No-Reply Mode

---

## 💡 คำแนะนำ

### สำหรับ Gmail ฟรี:
```javascript
const SENDER_NAME = "ชื่อธุรกิจของคุณ";
const REPLY_TO_EMAIL = "email@ที่มีคนดูแล.com";
const NO_REPLY_MODE = false;
```

### สำหรับ Google Workspace:
- ดูเอกสาร `EMAIL_SENDER_CONFIG.md` สำหรับวิธีใช้ GmailApp

---

## 🔍 การทดสอบ

### ทดสอบหลังแก้ไข:
1. Deploy version ใหม่
2. ทำการจองทดสอบ
3. ส่งไปที่ email ของตัวเอง
4. ตรวจสอบ:
   - Display Name ถูกต้องหรือไม่
   - กด Reply แล้วส่งไปที่ email ที่ตั้งไว้หรือไม่

---

## 📚 เอกสารเพิ่มเติม

- **รายละเอียดเต็ม:** อ่าน `EMAIL_SENDER_CONFIG.md`
- **คำถามที่พบบ่อย:** ดูใน `EMAIL_SENDER_CONFIG.md` ส่วน FAQ
- **Google Workspace:** ดูวิธีใช้ GmailApp ใน `EMAIL_SENDER_CONFIG.md`

---

**Quick Setup Version:** 1.0  
**Last Updated:** 2026-01-25
